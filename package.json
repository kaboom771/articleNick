{
  "name": "articlenick",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "b": "pnpm --filter @articleNick/backend",
    "w": "pnpm --filter @articleNick/webapp",
    "dev": "pnpm -r --parallel dev",
    "types": "concurrently 'pnpm b types' 'pnpm w types'",
    "prettify": "pnpm -r --parallel prettify"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "3.0.1",
    "rimraf": "5.0.1",
    "ts-node": "10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "5.9.2"
  }
}
